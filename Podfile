# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LearningTracks' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for LearningTracks
  pod 'GoogleSignIn'
  pod 'FBSDKLoginKit'
  pod 'Firebase/Auth'
  pod 'Firebase/Core'

  target 'LearningTracksTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LearningTracksUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
